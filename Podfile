# Uncomment the next line to define a global platform for your project
platform :ios, '11.0'

target 'SomeHostApp' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SomeHostApp
  pod 'FeatureNoby', :path => '../SomeModules/FeatureNoby'
  pod 'DesignSystem', :path => '../SomeModules/DesignSystem'
end
